# SQLite (файл относительно корня проекта, как при `prisma migrate`)
DATABASE_URL="file:./dev.db"

# Подпись JWT-сессии (≥ 32 символов). В продакшене обязательно свой секрет.
NUXT_AUTH_SECRET="your-random-secret-at-least-32-characters-long"
